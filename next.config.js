/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.pexels.com'] // Allow this domain in next.js
  }
}

module.exports = nextConfig
